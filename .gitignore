# Brand source files (2 GB of .ai/.psd/.zip) — reference material, never committed.
# Web-ready extracts go to packages/brand/assets/.
brandign-sanalys/

# Hookify rules are per-machine
.claude/*.local.md

# Dependencies and build output
node_modules/
.next/
.turbo/
dist/
*.tsbuildinfo

# Local env — values live only in the host env store
.env
.env.*

# OS / editor
.DS_Store
Thumbs.db

# playwright-cli snapshots and traces (docs/13-SKILLS.md §3)
.playwright-cli/

# Cuadros intermedios del generador de sueros (tools/drips). Los AVIF finales viven en
# web/public/img/drips/ y esos si se commitean; estos PNG son 67 MB de paso intermedio.
tools/drips/frames/
